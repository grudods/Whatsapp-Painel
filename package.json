{
  "name": "whatsapp-panel-webhook",
  "version": "1.0.0",
  "description": "Webhook para AutoResponder WhatsApp",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "18.x"
  }
}
